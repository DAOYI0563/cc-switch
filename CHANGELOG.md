# 变更记录

## 3.19.5 Skill 导入识别修复

- 点击“导入已有”时默认纳入该技能已发现的全部客户端，OpenCode 等端上的现存副本同样被识别。
- 同名副本的内容按客户端而异（例如各端的细微配置差异）视为同一 Skill 的合法差异：原样接管，不覆盖也不报错；删除、切换与手动同步也不再因跨端内容差异而阻断。
- 切换内容来源不再重置用户已勾选的导入目标。

## 3.19.4 Skill 导入冲突修复

- 导入已有 Skill 时默认只纳入明确选择的内容来源，不再隐式选中其他客户端的同名副本。
- 切换内容来源时同步重置导入目标，避免旧来源残留导致错误冲突。
- 忽略 WSL Skill 目录中的 Windows `:Zone.Identifier` 元数据，不再将其计为内容变化。
- 对真正不同的同名副本显示“与所选来源内容不同”，不再误称为外部修改。

## 3.19.3 Skill 管理修复

- 恢复 Skill 页“导入已有”入口，并在空状态中提供直接导入操作。
- 改为按需扫描 WSL Skill 目录，扫描期间显示明确进度，失败时不再误报为空。
- 将 Skill 扫描、导入、同步、启停和删除移入阻塞线程，避免卡住界面。
- 单个损坏的 `SKILL.md` 不再导致其他已有 Skill 全部无法导入。

## 3.19.2 WSL 专用重构

- 产品范围固定为 Windows 11 x64、WSL2 Ubuntu 用户 `zhldm`。
- 客户端固定为 Claude Code、Codex 和 OpenCode。
- 新增本地配置扫描、冲突中心和加密临时回滚。
- 新增手动 WebDAV sync-v3：逐记录合并、E2EE、设备生命周期、墓碑与 ETag CAS。
- 新增三端只读会话浏览与 Windows Terminal 恢复。
- 新增 CLI 状态与仅复制的升级命令。
- 新增每日离线 HTML 工作简报及受限加密同步。
- 固定简体中文并精简主导航、托盘和设置。
- 删除代理、路由、OAuth、故障转移、统计、Profiles、Deep Link、Updater、S3、OMO、非目标客户端和跨平台桌面包。
- 发布物改为单一 Windows x64 便携 EXE 和 SHA-256 校验文件。
